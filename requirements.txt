# Pinned to a set that resolves cleanly together (verified with `pip check`).
# gradio 6.x requires huggingface-hub 1.x, which in turn needs transformers 5.x
# and sentence-transformers 5.x — so these versions move together.
sentence-transformers==5.5.1
transformers==5.12.0
chromadb==1.5.9
groq==0.15.0
python-dotenv==1.2.2

# Query interface (Milestone 5)
gradio==6.18.0
# streamlit>=1.40.0

# Document ingestion (PDFs via pdfplumber)
pdfplumber==0.11.9
